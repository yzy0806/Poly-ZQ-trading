**Production readiness — September 12, 2026 (Asia/Taipei)**

1. **Release status.** Production trading remains stopped. The GitHub Actions image for commit `d5fee99c0936756502643b0e61cee5ac1b14c610` was pulled and verified, but its startup reconciliation fails. The local account-identity fix below must be committed, published through GitHub Actions, and verified in the resulting immutable image before promotion. Neither the existing production database nor the selected production image was replaced.

2. **Identified defect and fix.** The real IBKR execution bridge emitted a masked display label in `account_fingerprint`, while the execution coordinator expected the SHA-256 account identity used by the ledger. Replaying valid fills therefore produced identity conflicts for known orders. Execution callbacks and database identity now share the existing normalized SHA-256 implementation. Display-only account masking remains unchanged. Existing database identities are compatible; no purge, identity rewrite, or reconciliation override is required.

3. **Broker and hedge verification.** Authenticated IBKR and Polymarket reads from the VPS matched the staged ledger: 25 September ZQ, 12,153.8957 INC25 YES, and 24,307.5 INC50PLUS YES. No strategy orders were open. The hedge requirements were 12,153.75 and 24,307.50 shares respectively. The INC25 surplus was 0.1457 shares; INC50PLUS matched exactly. These observations are snapshots, not authorization to use cached reconciliation on startup.

4. **VPS checks.** The published image's Python source matched the committed Git source. The image runs as UID/GID 10001, includes the IBKR API, and passed database provenance validation on a disposable copy, market mapping and rule verification, ten synchronized Polymarket books, live September ZQ quotes, and a non-routing five-contract margin preview. Initial margin for the next batch was approximately $1,293 at the time of inspection. Configured limits remain five contracts per order, 60 total, one active batch, $100 minimum profit, 100 basis points minimum return, and 5% minimum margin cushion. The VPS full-excess-liquidity floor remains $10,000, versus $5,000 locally.

5. **Real startup regression.** The unmodified published image reproduced the mismatch against the live gateway. A second isolated container used the same image with the three changed source files mounted read-only and all order-submission flags disabled. It reached automated `CLEAN` reconciliation, reconciled all three positions, produced complete portfolio valuation and a current five-contract margin preview, and remained unarmed and unpaused. Only a scratch database was written. The source mounts are a validation preview, not a replacement production release.

6. **Automated validation.** The new tests exercise the actual callback bridge and execution coordinator, including valid account normalization, duplicate execution replay, and rejection of a different account sharing the same final four digits. The valid-account cases fail with the old masking behavior. All 319 Python tests, Ruff checks, formatting checks on changed Python files, and mypy pass. No live order was submitted. The dashboard was not changed.

7. **Geographic response.** The VPS eligibility endpoint returns country `NL` with `blocked=true`. Polymarket's current API-specific documentation distinguishes the Netherlands' frontend restriction from API availability, and the existing application policy permits this deployment country. The raw flag is retained for diagnostics. See [Polymarket geographic restrictions](https://docs.polymarket.com/api-reference/geoblock#regulatory-restricted-jurisdictions-close-only-on-frontend). This check does not establish account-specific authorization beyond the venue responses inspected.

8. **Database handoff.** The staged SQLite ledger passed integrity, foreign-key, opening-inventory, execution-provenance and live-position checks. Its SHA-256 is `418c85ff5e8beff1df7d76e4be864ef9c4ee0881d101972dd92002187605232f`. The original empty VPS ledger and its backup remain preserved. Keep both local and VPS trading stopped during activation; back up the current database, install the staged ledger with the verified successor image, then require fresh automated reconciliation before any separate arming action. Do not run the ordinary update script prematurely: it starts the engine immediately and does not install the staged database.
